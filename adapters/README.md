# adapters/

This directory holds PEFT **configs** (`adapter_config.json`) and placeholder notes committed for reference.

Trained weight files (`adapter_model.safetensors` / `.bin`) and `smoke_metrics.json` are **not** in this repo — they stay on the machine that ran train. Do not treat these folders as loadable adapters.
