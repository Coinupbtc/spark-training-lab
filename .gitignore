# Regenerable / huge — never commit (rebuild from adapters + base via scripts/)
/models/
*.gguf

# Python / tooling
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
*.egg-info/

# Secrets / local env
.env
.env.*
!.env.example

# Editor / OS junk
.DS_Store
.idea/
.vscode/
*.swp

# Transient train artifacts (keep runs/*.log; skip huge dumps)
runs/**/checkpoint-*/
runs/**/*.pt
runs/**/*.bin
node_modules/
